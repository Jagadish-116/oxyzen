services:
  - type: web
    name: oxyzen-music
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.12.0
      - key: PORT
        value: 8000
